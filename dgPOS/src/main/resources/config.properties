db.ip=192.168.0.192
db.port=3306
db.name=dgpos
db.user=root
db.password=jjgh5879921pomn##

# --- Backoffice bridge (StoreNET) ---
# Every till event is POSTed here; poller checks for manager force-close commands.
backoffice.url=http://192.168.0.192:3000
backoffice.enabled=true
store.id=14302
register.id=REG1

# --- Store identity (printed on receipts, shown on login/status) ---
# Per-store install — change every line below when deploying to a new store.
store.name=DOLLAR GENERAL
store.street=216 BELKNAP ST
store.city=SUPERIOR
store.state=WI
store.zip=54880
store.phone=(715) 718-6650
store.tax_rate=0.055

# --- Receipt printer (TCP ESC/POS) ---
printer.ip=192.168.0.179
printer.port=9100
